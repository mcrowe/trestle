- flash.each do |name, msg|
  = content_tag :div, class: "alert alert-#{ name.to_s == 'alert' ? 'warning' : 'success' } alert-dismissable" do
    button.close[type="button" data-dismiss="alert" aria-hidden="true"] ×
    = msg.html_safe